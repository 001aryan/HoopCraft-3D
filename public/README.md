Place your uploaded event image here so the site displays it:

- Save the image as `event-photo.jpg` and put it in this `public/` folder.
- The page expects the image at `/event-photo.jpg`.

If you'd like, I can add the file for you if you confirm the uploaded image is the one to use.